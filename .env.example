# =============================================================================
# Helper Platform — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# .env is listed in .gitignore — NEVER commit it to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (PostgreSQL)
# -----------------------------------------------------------------------------
DB_USERNAME=helper_admin
DB_PASSWORD=helper_secret_2026

# -----------------------------------------------------------------------------
# JWT SECRET
# Must be the same across ALL services — they all validate each other's tokens.
# Generate a strong secret: openssl rand -base64 64
# -----------------------------------------------------------------------------
JWT_SECRET=HelperPlatformDevSecretKey2026ChangeThisInProduction_AABBCC1234567890

# -----------------------------------------------------------------------------
# REDIS (optional password — leave empty for local dev)
# -----------------------------------------------------------------------------
REDIS_PASSWORD=

# -----------------------------------------------------------------------------
# EMAIL (SMTP)
# Used by: auth-service (OTP emails) and notification-service
#
# For Gmail:
#   1. Enable 2FA on your Google account
#   2. Go to: Google Account → Security → App passwords
#   3. Generate a 16-char app password and paste it below
# -----------------------------------------------------------------------------
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-16-char-app-password

# -----------------------------------------------------------------------------
# SMS (notification-service only)
# Provider: MSG91 (India) or Twilio
# Leave empty to keep SMS disabled (app.sms.enabled=false by default)
# -----------------------------------------------------------------------------
SMS_API_KEY=

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# File uploads (KYC docs, portfolios, invoices) are stored in Docker named
# volumes locally. For production, set APP_UPLOAD_STORAGE_TYPE=s3 in
# user-service and configure S3_BUCKET + AWS credentials.
#
# Firebase push notifications are disabled by default (app.firebase.enabled=false).
# To enable, add your firebase-service-account.json to helper-notification-service/
# src/main/resources/ and set app.firebase.enabled=true in application.properties.
